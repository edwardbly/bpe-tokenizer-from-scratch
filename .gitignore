# Jupyter autosave/checkpoint files — not meant to be tracked
.ipynb_checkpoints/

# Source text files — obtained separately per README instructions
# (see README for download links)
*.txt

# Common Python/OS clutter, in case any of these appear later
__pycache__/
.DS_Store